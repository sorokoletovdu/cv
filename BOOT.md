# CV Builder — AI Session Entry Point {#root}

## Session Start Protocol {#start}

**Read in this order before touching any code:**

1. This file (`BOOT.md`) — project context, invariants, REVIEW protocol
2. `WAL.md` — current project state, pending work, what NOT to touch
3. `docs/spec/INDEX.md` — full spec directory (read specific spec files as needed)

Tool-specific files (`CLAUDE.md`, `AGENTS.md`, `.github/copilot-instructions.md`) contain
tool-specific overrides only. All project-wide context lives here.

---

## What This Project Is {#overview}

A TypeScript + Astro CV builder. One Markdown file (`src/content/resume/resume.md`) is the
single source of truth for all outputs: web page (Astro → GitHub Pages), downloadable PDF
(`@react-pdf/renderer`, pure Node.js), and ATS feedback report (Claude API).

Full spec: `spec://cv/architecture#overview`

---

## Control Plane {#control-plane}

| File | Role |
|---|---|
| `BOOT.md` | This file — universal entry point for all AI sessions |
| `WAL.md` | Write-Ahead Log — session continuation state, pending work, REVIEW items |
| `docs/spec/INDEX.md` | Full spec directory index with URI → file mapping |
| `docs/spec/` | Domain-split specification files |

---

## Tech Stack Quick Reference {#stack}

| Concern | Choice |
|---|---|
| Package manager | **pnpm** — always use `pnpm`, never npm or yarn |
| Framework | Astro 5 with TypeScript strict mode |
| Styling | Tailwind CSS v4 — CSS `@theme` in `global.css`, no `tailwind.config.js` |
| PDF | `@react-pdf/renderer` — pure Node.js, no browser in CI |
| Fonts | `@fontsource/open-sans` — WOFF files from npm, loaded via `src/pdf/fonts.ts` |
| ATS check | `@anthropic-ai/sdk` via `scripts/ats-check.ts` |
| Data | Astro Content Collections — YAML front matter in `src/content/resume/resume.md` |
| CI/CD | GitHub Actions (3 workflows: deploy-pages, build-pdf, ats-check) |

Full spec: `spec://cv/stack`

---

## Key File Paths {#paths}

| File | Purpose |
|---|---|
| `src/content/resume/resume.md` | CV data (YAML front matter) — single source of truth |
| `src/content/achievements/achievements.md` | LinkedIn achievements — web-only, never in PDF |
| `src/content/config.ts` | Zod schemas for both collections |
| `src/pdf/` | React-PDF components (separate visual implementation from Astro) |
| `src/pdf/fonts.ts` | `Font.register()` — Open Sans WOFF from `@fontsource` |
| `src/pdf/theme.ts` | Design tokens: colors, font sizes, page margins |
| `scripts/generate-pdf.tsx` | Reads front matter → renders ResumePDF → `dist/CV.pdf` |
| `scripts/ats-check.ts` | Reads resume + JD → calls Claude API → `ats-report.md` |

---

## Architectural Invariants {#invariants}

These constraints are non-negotiable. Any change that violates them requires an explicit decision
recorded in `WAL.md` and reported to the author:

- Web (Astro) and PDF (React-PDF) share **data**, never components.
- PDF is generated by **pure Node.js** (`@react-pdf/renderer`). No browser in CI.
- `src/content/resume/resume.md` is the **single source of truth** for all outputs.
- CV layout is **single-column A4** — no tables, no multi-column, no inline icons.
- `src/content/achievements/` is **web-only** — never included in the 1-page PDF.
- Package manager is **pnpm** — never npm or yarn.
- TypeScript **strict mode** throughout.
- Tailwind is v4 — no `tailwind.config.js`. CSS-only configuration via `@theme`.

Full spec: `spec://cv/architecture#invariants`

---

## REVIEW Marker Protocol {#review-protocol}

When you encounter a spec ambiguity, a contradiction between two specs, or a decision you cannot
resolve without author input:

1. **Implement the spec as written** — do not invent alternatives or silently deviate.
2. **Add a REVIEW marker** at the relevant location in the file:
   ```
   <!-- REVIEW: [reason] — spec://cv/relevant#section -->
   ```
3. **Record the item** in `WAL.md` under `## Open REVIEW Items`.
4. **Report to the author** at the end of the session.
5. **Do NOT modify the spec unilaterally** — the author resolves spec conflicts.

If a spec file itself needs updating, note it as a REVIEW item. Never silently update specs
to match your implementation.

---

## Conflict Resolution Hierarchy {#conflict-resolution}

When instructions contradict, the higher authority wins:

1. Explicit author instruction in the current session
2. `docs/spec/` files (individual spec documents)
3. This file (`BOOT.md`)
4. Tool-specific files (`CLAUDE.md`, `AGENTS.md`, `copilot-instructions.md`)

---

## WAL Structure {#wal-structure}

`WAL.md` must always contain exactly these sections in this order:

| Section | Anchor | Content |
|---|---|---|
| Header | — | `Last updated: YYYY-MM-DD \| Session: <description>` |
| SESSION OPEN PROCEDURE | — | Blockquote — 3 mandatory steps; never modify |
| Current State | `#state` | Phase status table: all phases, one-line status per row |
| Pending Work | `#pending` | `[ ]` items with concrete commands or file paths; no vague tasks |
| Open REVIEW Items | `#reviews` | One entry per unresolved `<!-- REVIEW: -->` marker; empty section if none |
| Do NOT Touch | `#dntouch` | Bullet list of paths/files AI must never modify directly |
| Known Issues | `#issues` | Numbered list of active blockers; "No blocking issues" if none |
| Next Session Must Read | `#next` | **No conditional `if` instructions.** State what is in progress now with exact file paths and commands. If nothing is in progress: one sentence saying so. |

**Size constraint**: WAL must stay under 3,000 tokens. Completed items collapse to one-line
summaries. Details belong in git history, not WAL.

**Pending Work rules**: every item must be actionable on its own — include the exact command
to run, or the exact file and what to do in it. Never write "investigate X" or "check Y".

**Next Session Must Read rules**: written for cold-start. The AI reads it before the user
has stated a task. Conditional `if working on X` instructions are forbidden — the AI cannot
act on them. Write only what is unconditionally true: what is currently in progress, what
file and line to continue from, what to run first.

---

## Session Close Procedure {#close}

Before ending a session, update `WAL.md`:

1. Update the `Last updated` line with today's date and a one-phrase session description.
2. In `## Current State`, update any phase whose status changed.
3. In `## Pending Work`, check off completed items (`[x]`) and add new items discovered this session.
4. In `## Open REVIEW Items`, add any new `<!-- REVIEW: -->` markers created this session.
5. In `## Next Session Must Read`, describe the current in-progress state with exact file paths.
   If nothing is in progress, write one sentence: "No work in progress — ask the user."
6. Keep WAL under 3,000 tokens. Collapse or remove anything completed or resolved.
